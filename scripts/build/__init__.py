"""Loupe build pipeline package.

Experimental overhaul: gates, manifest, and (future) esbuild bundling
live here. ``scripts/build.py`` remains the release entry point; it will
delegate to this package incrementally.
"""